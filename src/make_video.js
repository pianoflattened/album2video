module.exports = async function makeVideo(form) {
    // run ffmpeg command that concatenates all the sound files (1/2)
    // run ffmpeg command that creates video with concat.wav and cover (2/2)
};
